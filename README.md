# Chenhao Zhang — Academic Homepage

Upload the contents of this folder to the root of `zhangchenhaoseu.github.io`.

Required structure:

```text
zhangchenhaoseu.github.io/
├── index.html
├── style.css
├── layout-fix.css
├── assets/
│   └── profile.jpg
└── files/
    └── Chenhao_Zhang_CV.pdf
```

The photo and CV are intentionally not bundled. Keep the filenames and paths exactly as shown above. Publication links are added only where a DOI was available in the supplied CV.

The publication section highlights five first-author SCI journal articles as representative works and separately lists collaborative publications, manuscripts under review or revision, and conference papers.
